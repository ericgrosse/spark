{
  "name": "spark-dating-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Cross-platform dating app monorepo for web, mobile, desktop, and API.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@eslint/js": "^9.6.0",
    "typescript-eslint": "^8.0.0"
  },
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "netlify:build": "npm --workspace @spark/api run prisma:generate && npm run test && npm --workspace @spark/mobile run typecheck && npm --workspace @spark/mobile run build"
  },
  "engines": {
    "node": ">=20.11.0",
    "npm": ">=10.0.0"
  }
}
